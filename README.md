Clone the project.
Add the firebase json file in the project root.
Run the command - npm install.
Run the command - node subscription-server.
Make sure the server is up and running on port 3000.
Click on the notification-sender.html and it will open simple form on browser.
Fill in all the fields and send the notification.
